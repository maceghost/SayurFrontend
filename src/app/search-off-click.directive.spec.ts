import { SearchOffCLickDirective } from './search-off-click.directive';

describe('SearchOffCLickDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchOffCLickDirective();
    expect(directive).toBeTruthy();
  });
});
